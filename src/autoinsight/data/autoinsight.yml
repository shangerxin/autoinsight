common:
  log_level:info
  log_format:
script:
  # script_root,
  output_root:script_root
ident:
  search_depth:50
  search_width:50
service:
  application launch timeout: 30
  launch retry interval:5
